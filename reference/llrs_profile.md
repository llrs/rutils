# Provide a R global user profile

Write my preferences on a R user profile and R environment.

## Usage

``` r
llrs_profile()
```

## Value

`TRUE` or `FALSE` depending on if it has overwritten the files or not.
